services:
  web:
    image: nginx:stable-alpine
    container_name: ohana-hp-web
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    restart: always